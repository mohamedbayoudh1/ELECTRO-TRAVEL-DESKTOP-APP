
<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        <a href="/" class="navbar-brand">Electro Travel</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="/" class="nav-link">Accueil</a>
                </li>

                {% if  app.user %}
                    <li class="nav-item">
                        <a href="{{ path('new_user')}}" class="nav-link">Ajouter Utilisateur</a>
                    <li class="nav-item">
                    <a href="{{ path('pdf_user')}}" class="nav-link">PDF</a>

                    </li>


                {% endif %}

                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_registration')}}" class="nav-link">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('security_login')}}" class="nav-link">Se connecter</a>

                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('security_logout')}}" class="nav-link">Se déconnecter</a>
                    </li>
                {% endif %}


            </ul>
        </div>
    </div>
</nav>